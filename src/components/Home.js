import React, {Component} from 'react';

class Home extends Component {
  render(){
    return(
      <div>
        <h1>Font Place</h1>
        <p>This is a marketplace layout example using react and react-router. I've used open source fonts as an example. Nothing actually downloads from this sites.</p>
      </div>
    );
  }
}

export default Home;
